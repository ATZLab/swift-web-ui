# 2026-06-17

## swiftwebui-bridge — v0.2.0 phase 2-4

- 11:30 `swiftwebui-bridge` — red tests for `JSClosureRegistry` map semantics
  + `BridgedClosure` value-type retain + `Values` primitive round-trip +
  `callAsPromise` + `Bridge.alert` / `Bridge.consoleLog` wrappers (commit
  `0271ccb`).
- 11:30 `swiftwebui-bridge` — green `JSClosureRegistry` (NSLock-guarded
  handle map) + `BridgedClosure` (~Copyable single-owner deinit) + `Values`
  (primitives host-runnable; array/object `os(WASI)`-gated) + `Promise`
  (`callAsPromise` async helper) + `ExternalJSAPI` (`Bridge.alert` /
  `Bridge.consoleLog` / `Bridge.fetch`) + `Sources/SwiftWebUIBridge/README.md`
  (ownership/async/binding DSL). 17 new host-runnable tests; 7 wasm32-only
  tests gated to `os(WASI)`. Native matrix: 48/48 green (commit `616cdef`).

## swiftwebui-docs — v0.2.0 phase 2-5

- 11:30 `swiftwebui-docs` — DocC catalog updated to reflect the v0.2.0
  `SwiftWebUIBridge` interop surface. New
  `Sources/SwiftWebUI/SwiftWebUI.docc/Articles/WebInterop.md` (titled
  'Calling JavaScript from SwiftWebUI') covers closure retain
  (`JSClosureRegistry` + `BridgedClosure`), value conversion (`Values`),
  Promise / async (`callAsPromise`), typed browser-global wrappers
  (`Bridge.alert` / `Bridge.consoleLog` / `Bridge.fetch`), and the
  binding DSL. `Essentials/GettingStarted.md` gains a 'Calling
  JavaScript' section that points at the new article; the status
  header and product-overview paragraph call out the 0.2.0 bridge
  module. `SwiftWebUI.md` (landing) adds a 'Bridge (SPI)' topic group
  and a `<doc:WebInterop>` cross-ref (commits `1b6265c`, `874d382`).
  DocC build: zero warnings. `swift test --no-parallel`: 48/48
  native green (no test changes — docs only). Cross-module article
  lives in the `SwiftWebUI` target catalog because
  `scripts/verify-docc.sh` only builds per-target archives
  (`--target`-mode).
