# 2026-06-17

## swiftwebui-bridge — v0.2.0 phase 2-4

- 11:30 `swiftwebui-bridge` — red tests for `JSClosureRegistry` map semantics
  + `BridgedClosure` value-type retain + `Values` primitive round-trip +
  `callAsPromise` + `Bridge.alert` / `Bridge.consoleLog` wrappers (commit
  `0271ccb`).
- 11:30 `swiftwebui-bridge` — green `JSClosureRegistry` (NSLock-guarded
  handle map) + `BridgedClosure` (~Copyable single-owner deinit) + `Values`
  (primitives host-runnable; array/object `os(WASI)`-gated) + `Promise`
  (`callAsPromise` async helper) + `ExternalJSAPI` (`Bridge.alert` /
  `Bridge.consoleLog` / `Bridge.fetch`) + `Sources/SwiftWebUIBridge/README.md`
  (ownership/async/binding DSL). 17 new host-runnable tests; 7 wasm32-only
  tests gated to `os(WASI)`. Native matrix: 48/48 green (commit `616cdef`).
