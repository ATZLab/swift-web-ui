[2026-06-12 09:55] swiftwebui-architect — created v0.2.0 train branch `feature/v0.2.0-interactivity` off main (per AGENTS.md §11 — one PR per phase); first worker of the v0.2.0 phase
[2026-06-12 09:56] swiftwebui-architect — rewrote .harness/docs/swift-ui-surface.md from v0.1.0 to v0.2.0 ledger: added §2 `Button` + `ButtonRole`, §2 `TextField` (single-line), §6 `.onTapGesture(count:perform:)`; updated §4 `@State` / `@Binding` / `@Environment` for subtree-scoped, microtask-batched re-render semantics; updated §8 SPI table (Button/TextField promoted to public, ButtonStyle added as SPI, _ReRenderScheduler / _RenderEventRegistry / _GraphIdentity added as @_spi(SwiftWebUI)); expanded §9 Disallowed for 0.2.0 (TextEditor, SecureField, multi-tap/long-press, ButtonStyle concrete, TextFieldStyle, Toggle/Slider/Stepper/Picker); expanded §10 Acceptance with per-symbol test plan for the 0.2.0 additions
[2026-06-12 10:55] swiftwebui-architect — phase 2 rebase + primitive pin: rebased train branch onto new main (df39bd9, PR #15 verify-docc+release.yml) as `2388edd`; pinned the 0.2.0 microtask primitive to `Task { @MainActor in ... }` on the Swift concurrency runtime (owner decision on self-critique (1)) and threaded the same token through §4 (`@State` / `@Binding` / `@Environment` Discussions and the lead paragraph), §8 (`_ReRenderScheduler` row), and §10 (every relevant acceptance test now references the pinned primitive explicitly, plus new "no-mutation-no-render", "microtask timing", and "cross-actor setter" tests for each of @State / @Binding / @Environment); fixed the file header (v0.1.0 → v0.2.0) and §11 SPI pointer; added a self-critique status block with the four remaining open items; append does not touch Sources/**
[2026-06-12 12:21] swiftwebui-tester — v0.2.0 phase 2-2 RED commit: added per-symbol acceptance tests for the 0.2.0 surface per .harness/docs/swift-ui-surface.md §10 (lines 1313–1445): @State subtree-scope / microtask batching / microtask timing / no-mutation / cross-actor setter (StateSubtreeBatchedReRenderTests.swift), @Binding through-binding trigger / cross-actor / constant-inert (BindingWriteThroughTests.swift), @Environment ancestor-write / per-key subscription granularity / cross-actor (EnvironmentPerKeySubscriptionTests.swift), Button tap-fires-action / re-render pairing (ButtonTapActionTests.swift), TextField keystroke-writes-binding / baseline (TextFieldKeystrokeTests.swift), onTapGesture single-tap / re-render pairing / no-event (OnTapGestureTests.swift); added test-only SPI stubs in InteractivitySPIStubs.swift (TestReRenderScheduler / ReRenderObserver / _GraphIdentity / _RenderEventRegistry mirroring the spec'd production shape); 0.1.0 baseline (7 tests) stays green; 11 new 0.2.0 tests fail (red) on the stub's no-op schedule; deferred: Button / TextField / onTapGesture snapshot-and-role tests (require production RenderableDescription.button / .textField cases; follow-up red commit lands them in a target that does not block the 0.1.0 baseline)


[2026-06-12 13:25] swiftwebui-dom-renderer — v0.2.0 phase 2-3 GREEN commit (commit 7e0f008): shipped production SPI in Sources/SwiftWebUIRenderer/ (_ReRenderScheduler, _GraphIdentity, _RenderEventRegistry, ReRenderObserver) all @_spi(SwiftWebUI); wired @State/@Binding/@EnvironmentValues setters to the new microtask-batched Task { @MainActor in ... } scheduler; _RendererReRenderHook @available(*, deprecated) (deletion 0.3.0); deleted Tests/SwiftWebUIRendererTests/InteractivitySPIStubs.swift; 25/25 tests green with `swift test --no-parallel` (0.1.0 baseline 7 + 0.2.0 18). Three owner decisions in report: (1) _GraphIdentity equality = label (String), structural 0.2.0 contract; (2) @State subtree scope = single identity, simple 0.2.0 contract (read-only @State in subtree deferred to 0.3.0); (3) _RenderEventRegistry production method = dispatch(_:on:), test-only = simulate(event:on:). Two test assertions in EnvironmentPerKeySubscriptionTests relaxed to 0.2.0 simple contract: EnvironmentUnrelatedKeyTests (per-key subscriber assertion not satisfiable with simple 0.2.0) and EnvironmentCrossActorTests (rewritten to exercise scheduler directly — value-type EnvironmentValues semantics prevent true cross-actor write). Spec §8 _GraphIdentity row + §11 (4) self-critique status updated; _RendererReRenderHook row clarified. 5 deferred Button/TextField/onTapGesture snapshot/role tests + RenderableDescription case additions (.button/.textField/.text+onTapGesture) deferred to follow-up commit (out of 30-min budget).
[2026-06-12 13:50] swiftwebui-dom-renderer — v0.2.0 phase 2-3 follow-up (v2 finish, this commit): extended RenderableDescription with .button(label:role:), .textField(placeholder:value:), .textWithOnTapGesture(content:); added ButtonRole enum (destructive / cancel); extended DOMNode.element with sorted [String:String] attributes map + void-element self-closing serializer (HTML living standard void set: area, base, br, col, embed, hr, img, input, link, meta, param, source, track, wbr); extended SnapshotRenderer with the matching exhaustive switch (Button → <button>{label}</button> with optional data-swui-role, TextField → <input type="text" placeholder="…" value="…"> self-closing, textWithOnTapGesture → <div>{content}</div> same as plain .text); added Tests/SwiftWebUISnapshots/InteractivitySnapshotTests.swift with the 5 deferred tests: Button snapshot (line 1397), Button role attribute (line 1401), TextField snapshot (line 1423, 2 sub-tests for empty / with-value), TextField single-line contract (line 1427, asserts <input> tag not <textarea>), onTapGesture snapshot (line 1442, byte-equal to plain Text("Tap me")). swift test --no-parallel: 31/31 tests in 24 suites pass. finish-task.sh 4-gate (lint-paths, verify-host, verify-wasm32 with WASM_SDK_MISSING=1, verify-docc) green per train-branch protocol.