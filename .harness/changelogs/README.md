# Changelog — bootstrap

This file is a placeholder; it will be replaced by per-day files
(`YYYY-MM-DD.md`) as work lands. Format used in those files:

```markdown
---
[2026-06-11 14:22] <rein-name> — <one-line summary>
```

The `swiftwebui-steward` agent aggregates these into
`ReleaseNotes.md` at tag time.
