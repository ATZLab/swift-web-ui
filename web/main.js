// web/main.js
//
// Dev placeholder. The real JS bootstrap (loading the wasm binary,
// registering JS ↔ Swift glue) lands once the renderer/bridge
// surfaces are wired up. For now this just logs to the console so
// `scripts/serve.sh` has a non-empty `web/` to serve.
//
// Owner: swiftwebui-tooling.
console.log("SwiftWebUI: dev placeholder loaded");
